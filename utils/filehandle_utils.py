def getFileNames(input_path : str) -> list[str]:
    """
    param : 
    input_path -> location of the unsorted image files
    
    The function go through the contents in the input path and gather all the file names
    in the directory. The file names are then sorted based on their name and stored in a list.
    The list is then returned.

    return : 
    file_names -> list of image files names in the input path 
    """
    file_names = []
    
    return file_names